name: Tests
on: push
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Change Directories
        run: cd os-in-browser
      - name: Install modules
        run: npm install
      - name: Run tests
        run: npm test
