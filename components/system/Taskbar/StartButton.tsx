import type { FC } from 'react';
import StyledStartButton from 'styles/components/system/StyledStartButton';

const StartButton: FC = () => <StyledStartButton>s</StyledStartButton>;

export default StartButton;
